<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CoolTimer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CoolTimer" parent="Base.Theme.CoolTimer" />

    <style name="MySeekBar">
        <item name="android:progressBackgroundTint">@color/darkerBackgroundApp</item>
        <item name="android:progressTint">@color/seekBarCircle</item>
        <item name="android:colorControlActivated">@color/seekBarCircle</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>
</resources>